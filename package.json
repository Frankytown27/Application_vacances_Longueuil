{
  "name": "application-vacances-longueuil",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "format": "biome format .",
    "lint": "biome lint .",
    "typecheck": "npm run typecheck -w apps/web && npm run typecheck -w apps/api",
    "test": "npm run test -w apps/web && npm run test -w apps/api"
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.0"
  }
}
